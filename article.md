# 从一行弹幕到一个3D旋转球体：我用Python写了个「全屏攻击」提醒工具

> 一个喝水提醒，被我做成了3D粒子特效。

## 需求：我要一个「躲不开」的提醒

我每天坐在电脑前写代码，经常一坐就是三四个小时。定了好几个喝水提醒、站立提醒，但它们都安安静静地躺在系统托盘里，偶尔弹个通知栏气泡——然后我继续敲键盘，完全没看见。

我需要一个**躲不开**的提醒。一个真正能「打」到我脸上的东西。

**需求清单：**
1. 全屏覆盖，任何窗口上都显示
2. 支持鼠标穿透，完全不干扰当前操作
3. 弹幕式动画，足够醒目
4. 常驻系统托盘，开机自启
5. 配置灵活：间隔、样式、生效时段
6. 窗口黑名单：全屏游戏时自动静音

于是 `Danmaku Reminder` 诞生了。

---

## 规划：为什么是双窗口架构？

画架构图的时候，最核心的决策是**职责分离**。

整个系统分为两层：

```
┌──────────────────────────────────────────────────┐
│                  main.py                         │
│  ┌─────────────┐  ┌──────────────────────────┐  │
│  │  Scheduler   │  │  DanmakuApi (JS Bridge)  │  │
│  │  threading   │  │  Python ↔ JS 双向调用     │  │
│  │  Timer 引擎   │  └───────────┬──────────────┘  │
│  └──────┬───────┘              │                 │
│         │                      │                 │
│         ▼                      ▼                 │
│  ┌──────────────────────────────────────────┐    │
│  │              SQLite 数据库                │    │
│  │  reminders / schedule_rules /            │    │
│  │  window_blacklist / settings             │    │
│  └──────────────────────────────────────────┘    │
│         │                      │                 │
│         ▼                      ▼                 │
│  ┌──────────────┐  ┌────────────────────────┐   │
│  │ Overlay 窗口  │  │ Settings 窗口          │   │
│  │ 全屏透明      │  │ frameless 设置页       │   │
│  │ 只渲染弹幕    │  │ 只管理配置             │   │
│  └──────────────┘  └────────────────────────┘   │
└──────────────────────────────────────────────────┘
```

### Overlay（覆盖层）

全屏、透明、点击穿透。它的唯一使命是渲染弹幕。用户在它「后面」正常操作电脑，完全感知不到它的存在——直到提醒触发。

### Settings（设置窗口）

常规 UI 窗口，管理提醒列表、全局设置、窗口黑名单。

### JS Bridge：前后端的「电话线」

两个窗口通过同一个 `DanmakuApi` 实例与 Python 通信。pywebview 的 `js_api` 机制自动暴露 Python 方法给 JavaScript 调用。

最妙的设计是**设置实时同步**：

```
Settings 拖动滑块 → Python 写入 SQLite → evaluate_js 推送到覆盖层
```

整个过程不到 50ms，用户每次拖滑块，弹幕效果即时变化。

### Scheduler 引擎

每个提醒对应一个 `threading.Timer`。到点了触发回调，重新计时，无限循环。

Snooze 的实现：取消当前 Timer → 创建一个 n 分钟后的一次性 Timer → 覆盖层显示推迟按钮 → 关闭点击穿透等待用户点击 → 8 秒无操作自动恢复穿透。

### 技术选型

| 方案 | 选它不选其他的原因 |
|------|-------------------|
| **pywebview** | 轻量（无额外依赖），用系统 WebView2，JS Bridge 天然适合 Python+HTML 架构 |
| **SQLite** | Python 内置，零配置，四张表搞定所有数据 |
| **Win32 ctypes** | 控制窗口样式、点击穿透、前台窗口检测 |
| **Web Animations API** | 浏览器原生动画引擎，60fps 硬件加速 |

整个项目外部依赖只有 `pywebview` 一个包。

---

## 实现：从简单到极致

### 第一版：会飘的字

```python
self.overlay = webview.create_window(
    "DanmakuReminder", self._danmaku_url,
    transparent=True, fullscreen=True, on_top=True,
)
```

三行代码，一个全屏透明窗口。HTML 里跑着弹幕动画。

弹幕分两种模式：**滚动**（CSS animation 从右往左飘）和 **居中弹入**（Web Animations 缩放 + 脉冲发光）。

效果还行。但不够。

### 全屏攻击：三段式粒子动画

滚动太温和了。我要的是**攻击感**。

`fullscreen_attack` 模式的三段式动画：

**Phase 1 — Gather（聚拢）**：80~100 条弹幕从屏幕四周边界同时飞出，飞向屏幕中心聚拢。聚拢过程中文字从 100% 缩放逐渐缩小到几乎不可见的光点。

**Phase 2 — Spin（旋转）**：所有光点排列成一个 3D 球体，围绕 Y 轴缓缓自转。Z 轴深度控制缩放和透明度——面向屏幕的光点更大更亮，背向的更小更暗，产生立体感。

**Phase 3 — Explode（爆炸）**：球体向外炸开，光点在飞行过程中从点放大到正常文字大小，铺满整个屏幕。

这个「点→球→爆炸」的三段式设计，在一开始就定好了时间分配（15% 聚拢 → 54% 旋转 → 31% 爆炸），然后用 `el.animate()` 预计算 90 帧关键帧，运行时零计算开销。

### 3D 球体的数学

每条弹幕用一个方向向量 `(nx, ny, nz)` 表示在单位球面上的位置，乘以半径 `r` 得到三维坐标，围绕 Y 轴旋转后投影到 2D 屏幕：

```javascript
function spherePoint(p, rotAngle) {
    var rx = r * p.nx, ry = r * p.ny, rz = r * p.nz;
    // Y 轴旋转矩阵
    var xr = rx * cosA + rz * sinA;
    var zr = -rx * sinA + rz * cosA;
    return { x: cx + xr, y: cy + ry, z: zr };
}
```

**第一版用了随机经纬度分布，导致粒子在两极点聚集，球体很难看。**

改用**斐波那契球体算法**后，问题解决了：

```javascript
var goldenRatio = (1 + Math.sqrt(5)) / 2;
for (var a = 0; a < ATTACK_COUNT; a++) {
    var fi = a / ATTACK_COUNT;
    var uy = 1 - 2 * fi;
    var theta = 2 * Math.PI * a / goldenRatio;
    var rAtY = Math.sqrt(1 - uy * uy);
    var ux = rAtY * Math.cos(theta);
    var uz = rAtY * Math.sin(theta);
}
```

用黄金比例在球面上均匀采样，每个粒子占据相等的表面积——效果从「一堆乱码」变成了「一颗星球」。

### 深度迭代：每个参数都调过很多次

**球体半径**：20px → 200px → 400px → 最后加了滑块 100~800px 随便调。

**光点大小**：从 `0.15~1.50` 一路压到 `0.04~0.20`，50px 的文字在球面上缩成 2~10px 的彩色光点，刚好能看见颜色但读不出是什么字。用户喊停的位置。

**旋转速度**：6 圈/3 秒 → 2 圈 → 最后 1 圈，星球般优雅自转。

### 字体颜色：渐变与纯色

完整的颜色选择器：

- **渐变模式**：11 种渐变色预设（红橙金翠绿天蓝紫粉青珊瑚亮绿），每次弹幕随机取色
- **单色模式**：9 种纯色预设 + 自定义颜色取色器

设置即时生效，不需要重启。

它的技术实现覆盖了 `background-clip: text`、`-webkit-text-fill-color`、`linear-gradient` 之间的互操作——渐变文字和纯色文字走了完全不同的 CSS 路径。

### 设置实时同步

操作逻辑：全局设置的每个滑块，在拖动时通过 JS Bridge 调用 `update_overlay_setting(key, value)`。Python 端收到后执行 `evaluate_js("updateSetting('{key}', '{value}')")`，覆盖层立刻更新。

这里踩过一个坑：`evaluate_js` 传字符串参数时忘记加引号，导致速度滑块的值变成了变量名 `gradient`（抛 ReferenceError）。修起来一行代码，但暴露了一个架构问题——所有设置值走同一个管道，但数字和字符串的 JS 语法不同。

---

## 达到的效果

最终产品是一个常驻系统托盘的桌面工具，具备以下能力：

**弹幕三种模式**：
- 滚动飘过 → 不打扰，适合轻度提醒
- 居中弹入 → 加粗大字 + 发光脉冲，适合中度提醒
- 全屏攻击 → 3D 球体旋转 + 爆炸扩散，极重度提醒（默认模式）

**智能提醒**：
- 重复触发（一次提醒连发 N 次）
- Snooze 推迟（弹窗 8 秒，或推迟 n 分钟后再次提醒）
- 窗口黑名单（全屏游戏自动静音）
- 生效时段（只在工作日/工作时间提醒）

**实时调节**：
- 透明度、速度、字号、弹幕数量、球体半径、字体颜色——全部滑块即时生效

全部代码只有 **600 行 Python + 400 行 JavaScript**。外部依赖：1 个。

---

## 复盘

这个项目从「一行弹幕」开始，经过几十次迭代，变成了一个带着 3D 旋转球体的全屏攻击提醒工具。

几个关键决策影响了项目的走向：

**双窗口架构**在第一版就搭好了。后面所有的功能——设置实时同步、snooze、球体半径调节——都是这个架构的自然延伸。**架构对了，功能只是往架子上放东西。**

**前端选型用原生 JS + Web Animations API 而不是 React/Vue**。因为弹幕渲染不需要虚拟 DOM，`el.animate()` 直接操作合成器线程，性能比框架好得多。

**斐波那契球体算法**是让球体从「难看」变「好看」的关键一步。它的数学优美且实现简单——几行代码，效果天壤之别。

**每次用户说「改一下」，都只调一个参数。** 半径大了小了、快了慢了、亮了暗了——每次只改一个值，用户立刻看到效果。这种即时反馈的迭代节奏让产品快速收敛到「正好」的位置。

---

*项目核心代码开源在 GitHub，600 行 Python + 400 行 JavaScript。你可能不需要一个全屏攻击的提醒工具，但下次写小工具的时候，想想 pywebview + Web Animations API + SQLite 这个组合——它能做到的事比你以为的多得多。*
